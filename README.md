[English Version](docs/README_EN.md) | 简体中文

<img width="1516" height="1523" alt="icon" src="https://github.com/user-attachments/assets/f3b678ec-0c1d-4edc-9644-9b5537762d9a" />


# VocalMap 官方正式版 🚀


欢迎来到 VocalMap 的正式商业版本！这是一款专为声乐教学、歌手训练和专业音频制作打造的桌面级声乐诊断与音轨分离工作站。

在经历了一系列的底层架构重构与极限性能压榨后，我们为你带来了目前最先进的**纯粹、极简、零延迟**本地化 AI 音频处理体验。最新版本全面进化，不仅巩固了桌面端的性能，更**正式跨平台登陆 Android 移动端**，让您的每一次发声都能随时随地留下具象的痕迹！

## 🌟 核心亮点功能

### 1. 全方位纯 Rust IPC 驱动架构 (全新重磅升级)
我们彻底废弃了曾经沉重且极易被破解的 Python FastAPI 框架，将其底层架构 100% 迁移至 Tauri 原生 Rust IPC 环境：
*   **10MB 极限身材**：告别动辄 500MB 的笨重安装包，由于剔除了所有本地捆绑的 Python 依赖，新版安装包体积直接降维打击至 **< 10MB**！所有的 AI 运行环境将在首次使用时通过云端“按需极速拉取”。
*   **零延迟内存交互**：完全抛弃了 WebSocket 网络传输开销，声学引擎现在直接通过内存级 Rust IPC 与 WebAudio 通信，音高追踪与诊断延迟降低至微秒级。
*   **无法攻破的黑盒安全**：再也不用担心本地端口被恶意监听！核心鉴权、DSP 解析器（包括高精度 YIN 算法）现已全部被重写为 Rust 原生机器码，带来不可阻挡的逆向防爆屏障。
*   **跨平台 Android 适配**：除了 Windows 平台，现已全面支持 Android 移动端。通过深度优化的 Webview 响应式布局与触摸交互，将专业的声学训练装进口袋。

### 2. 进阶靶向发声训练营
摒弃无目的的瞎唱，系统将基于您选择的标准声部（或高度自由的**自定义音域**），为您动态生成极具针对性的 **6 大渐进式声学练声关卡**：
*   **3 秒动态预判运镜 (Look-ahead Camera)**：彻底解决大跨度音阶（如从胸声直飙头声）导致目标飞出屏幕的视觉盲区，相机视野平滑智能跟随，训练无死角。
*   **100ms 视觉抗延迟补偿算法**：底层重构了“视听同步”逻辑。钢琴准时播放，判定方块巧妙退后 100 毫秒以完美抵消麦克风与系统的收音计算延迟，感受极致精准的命中手感。
*   **录音棚级双轨压限引擎**：内置 Web Audio 动态压限器 (Dynamics Compressor)，彻底解锁并放大了钢琴提示音的极限声量，再大声也圆润厚实，告别失真爆音 (Hard Clipping)。
*   **新增【自定义靶向特训】入口**：彻底打通从分离到特训的生态闭环！您可以导入由 AI 音轨分离功能提取的专属 `.tmap` 自定义关卡文件，针对性攻克您最想练的任意高难度歌曲片段。

### 3. 实时声学多维诊断引擎
利用零延迟的 **Rust 原生高性能信号分析算法**（YIN 音高追踪变体 + H1-H2 共鸣度评估），VocalMap 能够通过你的麦克风实时捕捉并解析声音特征。
*   **六维全息诊断雷达 (Pro 版本功能)**：毫秒级抓取音准、稳定度、共鸣度、纯净度等高阶发声指标，甚至能精准捕捉专业级颤音（Vibrato）并独立评分。
*   **动态复盘回放**：所有演唱轨迹均可一键打包为 `.vmap` 项目文件。回放时视角中心会自动锁定当前音高，配合高精度的白色基准点与纵向标尺，让你像拿放大镜一样看清每一次气息抖动。
*   **高清长图留存**：一键无缝导出暗黑质感的高清长图诊断报告，拒绝背景发灰或内容裁切，成果分享一气呵成。

### 4. 录音棚级专业音轨分离 (Pro 版本功能)
搭载了当前 SOTA 级别的 **BS-RoFormer** 与 **Logic-RoFormer** 深度学习分离模型。
*   **卡拉OK 人声/伴奏分离**：一键剔除原唱，保留极度纯净的伴奏与和声。
*   **全能 6 轨道乐器分离**：将混合混音精准解剖为 人声、钢琴、吉他、贝斯、鼓组 与 其他。
*   **双核导出特训包**：分离完毕后，系统不仅能导出用于回放的 `.vmap` 标准复盘包，更可基于人声干音自动提取离散的块状音高序列，导出为 `.tmap` 靶向特训关卡。
*   **绝对的数据安全**：纯本地 GPU/CPU 推理，不上传服务器，绝无数据泄露风险。

### 5. 次世代极客质感交互
*   全面采用液态玻璃（Liquid Glass）高质感 UI，并包含暗色/亮色双模式。
*   所有交互动效附带平滑渐变反馈；**支持响应式自适应布局**，无论是大屏高分辨率还是窄窗/竖屏，画布及核心仪表均能基于 Flexbox 弹性填充和防清空重绘，完美规避排版灾难，让工具的使用成为一种沉浸式的享受。
*   **独立的无遮挡激活/状态面板**：彻底重构了 Pro 权限激活与状态查看逻辑，摒弃了传统的全局遮罩（Overlay），改为全尺寸独立工作区平滑流转。

### 6. 全球化多语言与无感更新体验
*   **中/英双语模式与持久缓存**：新增系统语言自动识别（首次启动根据系统区域设置自动初始化为中文/英文）并持久化缓存用户选择。所有的功能界面、高级参数调节面板以及**完整的使用帮助与诊断手册**都已实现 100% 的多语言本地化。
*   **无感弹窗式 OTA 升级**：顶部导航栏升级小字提示已全面移除。升级进度、下载百分比、更新失败警告以及重启引导均改用精美的 Toast 弹窗通知。

---

## 📥 如何安装与使用

1.  下载下方的 `VocalMap.Setup.x.x.x.exe` 安装包并运行。
2.  进入应用后，首页的基础功能完全免费。高阶的多维分析与音轨分离等核心能力将在支付后开通（月卡￥19.9，终身会员￥59.9，一次购买，新功能终身免费升级）。
3.  **针对国内环境大幅优化**：首次使用 Pro 版本的 AI 功能时，程序会自动从**国内高速专线镜像源**为您部署底层的独立轻便运行环境（免安装配置）。
4.  部署完毕后，尽情享受极速本地 GPU/CPU 加速带来的极致视听体验吧！

> [!NOTE]
> 如果您的电脑拥有 NVIDIA 显卡，程序将在下载环境后自动识别并启用 CUDA 并行加速，复杂音轨分离以及音频推演的速度将获得数十倍的提升！

## 📱 移动端（Android）现已正式支持

经过技术团队的不懈努力，VocalMap 现已正式完成对 **Android 移动端** 的移植支持，不仅完美继承了桌面端的高质感 UI 交互，更在移动端重写了底层安全机制与文件流转逻辑。
* **原生跨平台架构**：保留 Tauri v2 + Rust 原生 DSP 引擎的核心处理链路，所有音高追踪分析性能（YIN 算法等）在手机端体验依然如丝般顺滑。
* **移动端专属特性优化**：深度对接 Android 原生 API，我们已经重构了底层特征鉴权模块 (通过 `ANDROID_ID`)，并全面适配了移动端的作用域分区存储权限。
* **精简移动端功能**：**AI 音轨/伴奏分离模块（BS-RoFormer 模型）由于高强度的硬件算力门槛，已在移动端被专门剔除与禁用**，但对应的格式转换与核心练习功能完全保留。
* **移动端专用优化与适配**：
  * **界面布局自适应 (R1)**：通过升级视口 `<meta>` 标签防止移动端页面缩放，并优化了 CSS 媒体查询以适配 900px 以下移动端视口与无鼠标悬停粗指针设备。
  * **流畅高帧率渲染 (R2)**：优化 Canvas 渲染性能，将原来性能模式独享的离屏背景 Canvas 缓存机制（Offscreen Cache）推广到非训练的所有模式，并将每一帧的渲染全部交由 `requestAnimationFrame` 循环，同时在 Android 端智能避开高开销的 `shadowBlur` 阴影模糊绘制，实现稳定流畅的高帧率音高追踪画面。
  * **窗口固定与防抖动 (R3)**：锁定 `html, body` 绝对宽度/高度为 `100%` 并设为 `fixed` 固定定位，通过阻止 `touchmove` 事件默认行为完美规避了移动端特有的橡皮筋/视口拖拽回弹现象。
  * **本地依赖逻辑绕过 (R4)**：由于移动端已移除离线 Python AI 推理，因此在前端 `separation.js` 及后端 Rust `check_dependencies` 均直接绕过并跳过本地 AI 环境的下载和完整性校验逻辑。
  * **安全云端支付路由 (R5)**：专门为移动端设计了云端创建订单路由。支付模块不再直连外部 Web API，而是通过 Rust 后端的 `vmap_request_payment` 命令代理请求，安全生成并跳转至外部支付通道。

关于具体的移植架构与重构详情技术文档，参阅：[Android 移动端移植重构记录](docs/ANDROID_FEASIBILITY.md)

## 📄 版权与安全合规声明
本项目（包括但不限于 Tauri 前端外壳、Rust 原生后端引擎、核心声学分析算法与独立的 AI 分离模型模块）拥有完整的自主知识产权。
*   **非商业限制**：本项目采用 **PolyForm Noncommercial License 1.0.0** 许可协议授权。任何组织或个人未经官方书面商业许可，不得将本项目、代码或编译产物用于任何商业目的。
*   **严禁破解与非法二次分发**：本软件内置的安全校验（如 RSA 原生加密校验、系统时钟防作弊检测等）为法定的“技术保护措施”。任何故意避开、破解或破坏这些技术保护措施的行为均属违法。
*   **法律维权声明**：对于任何故意破坏技术保护措施、二次分发破解版本、或越权商业化侵权的行为，我们将保留通过平台 DMCA 强制下架、民事诉讼索赔及追究刑事责任（侵犯著作权罪）的全部法律权利。

## 🛠️ 软件开发教程
见[README_CORE](docs/README_CORE.md)